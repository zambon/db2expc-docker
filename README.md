# IBM DB2 Express-C image for Docker

This is the Git repo for Dockerfiles and scripts for IBM DB2 Express-C, maintained by the IBM Analytics Emerging Technology team.

The Dockerfile in this directory is used to build the `DB2 Express-C` image on [Docker Hub](https://registry.hub.docker.com/u/ibmcom/db2express-c/)

# Usage
Instructions for using the image can be found on [Docker Hub](https://registry.hub.docker.com/u/ibmcom/db2express-c/). It is possible to build the image yourself by cloning this repository, changing to the `db2expc-docker/kernel-2.6.32` or `db2expc-docker/kernel-3.10.0` directory and then issuing the command `docker build .`
